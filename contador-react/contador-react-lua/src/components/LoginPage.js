// LoginPage.js
import React from 'react';

const LoginPage = ({ login }) => (
  <div>
    {/* Seu conteúdo da página de login */}
    <button onClick={login}>Login</button>
  </div>
);

export default LoginPage;
